{
  "name": "nodejs-proxy-modsbots",
  "version": "1.0.0",
  "description": "this is what we want",
  "main": "app.js",
  "scripts": {
    "build": "babel src -d dist", // Add this line, adjust command as needed
    "start": "node dist/app.js"
  },
  "dependencies": {
    "http-proxy": "latest",
    "ws": "^8.13.0"
  }
}
