{
  "name": "nodejs-proxy-modsbots",
  "version": "1.0.0",
  "description": "this is what we want",
  "main": "app.js",
  "scripts": {
    "start": "node main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-proxy": "latest",
    "ws": "^8.13.0"
  }
}
